@echo off
echo Clearing logcat buffers...
adb logcat -c

echo Capturing main buffer...
adb logcat -b main > main_log.txt

echo Capturing system buffer...
adb logcat -b system > system_log.txt

echo Capturing events buffer...
adb logcat -b events > events_log.txt